# Time Page Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build and publish a minimal static frontend page that shows the current date and time and refreshes every second.

**Architecture:** The project uses one static HTML file for markup, styling, and behavior, plus one small Node verification script to check the required page structure before publish. Local browser QA validates rendering and time updates.

**Tech Stack:** HTML, CSS, JavaScript, Node.js, Python standard library HTTP server, Git, GitHub CLI

---

### Task 1: Add failing verification

**Files:**
- Create: `scripts/verify-page.js`
- Test: `scripts/verify-page.js`

- [ ] **Step 1: Write the failing test**

```js
const fs = require("node:fs");

const html = fs.readFileSync("index.html", "utf8");
const requiredChecks = [
  { label: "title", test: /<title>Current Time<\/title>/i },
  { label: "heading", test: /<h1[^>]*>Current Time<\/h1>/i },
  { label: "clock target", test: /id="time"/i },
  { label: "date target", test: /id="date"/i },
];

const missing = requiredChecks.filter((entry) => !entry.test.test(html));

if (missing.length > 0) {
  console.error(`Missing required page elements: ${missing.map((entry) => entry.label).join(", ")}`);
  process.exit(1);
}

console.log("Static checks passed.");
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node scripts/verify-page.js`
Expected: FAIL because `index.html` does not exist yet.

- [ ] **Step 3: Commit after green later**

This commit happens after implementation and verification, not yet.

### Task 2: Implement the page

**Files:**
- Create: `index.html`
- Modify: `scripts/verify-page.js`

- [ ] **Step 1: Write minimal implementation**

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Current Time</title>
  </head>
  <body>
    <main>
      <h1>Current Time</h1>
      <p id="time"></p>
      <p id="date"></p>
    </main>
    <script>
      const timeElement = document.getElementById("time");
      const dateElement = document.getElementById("date");

      function renderTime() {
        const now = new Date();
        timeElement.textContent = now.toLocaleTimeString();
        dateElement.textContent = now.toLocaleDateString(undefined, {
          weekday: "long",
          year: "numeric",
          month: "long",
          day: "numeric",
        });
      }

      renderTime();
      setInterval(renderTime, 1000);
    </script>
  </body>
</html>
```

- [ ] **Step 2: Run test to verify it passes**

Run: `node scripts/verify-page.js`
Expected: PASS with `Static checks passed.`

### Task 3: Browser validation and publish

**Files:**
- Modify: `.gitignore` if needed
- Create: `.gitignore`

- [ ] **Step 1: Run local static server**

Run: `python -m http.server 4173`
Expected: local server starts at `http://127.0.0.1:4173`

- [ ] **Step 2: Validate in browser**

Check that the page loads, shows the heading, and the time text changes after at least one second.

- [ ] **Step 3: Commit intended files**

```bash
git init
git add index.html scripts/verify-page.js docs/superpowers/specs/2026-05-24-time-page-design.md docs/superpowers/plans/2026-05-24-time-page.md .gitignore
git commit -m "feat: add static time page"
```

- [ ] **Step 4: Create and push remote**

```bash
gh repo create test-fronted-github --public --source . --remote origin --push
```
